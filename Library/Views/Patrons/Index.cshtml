@{
  Layout = "_Layout";
}

<h2>Patrons</h2>
<a href="/patrons/new">Add patrons to patron list</a>
<a href="/">Return to Home Page</a>
<!-- <div>
  <form method="post" action="/patrons/search">
    <label for="search-input">Search Books</label>
    <input type="text" name="search-input" class="form-control">
    <button type="submit" class="btn btn-danger">Search</button>
  </form>
</div> -->
@if (@Model.Count > 0)
{


  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Name</th>
        <th>Details</th>
      </tr>
    </thead>
    <tbody>
      @foreach(var patron in @Model)
      {
        <tr>
          <td>@patron.GetFirstName() @patron.GetLastName()</td>
          <td><a href="/patrons/@patron.GetId()/details">Details</a></td>
        </tr>
      }
    </tbody>
  </table>

  }
